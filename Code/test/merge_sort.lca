program merge_sort {
	
	var arr:[int](4) = {20,10,2,5}
	var helper:[int](4)
	var arr_len:int = 4

	function tryy(low: int, high: int){
		var i:int = 0

		for(i = low; i <= high; i += 1)do
    		helper[i] = arr[i]
    		print(i)
    		print(high)
    	end

	}

	function merge(low: int, middle: int, high: int){
    	var i:int = 0
    	var j:int = 0
    	var k:int = 0


    	for(i = low; i <= high; i = i + 1)do
    		helper[i] = arr[i]
    	end

    	i = low
    	j = middle + 1
    	k = low

    	while( i <= middle and j <= high) do
    			print("main 1 K")
    			print(k)
    			print("main 1 i")
    			print(i)
    		if(helper[i] <= helper[j])do
    			arr[k] = helper[i]
    			print("while 1 i")
    			print(i)
    			i = i + 1
    		end
    		else do
    			arr[k] = helper[j]
    			print("while 1 j")
    			print(j)
    			j = j + 1
    		end
    		print("while 1 k")
    		print(k)
    		k = k + 1
    	end

    	while( i <= middle)do
    		arr[k] = helper[i]

    		k = k + 1
    		i = i + 1

    		print("while 2 k")
    		print(k)
    		print("while 2 i")
    		print(i)

    	end

    }

	function merge_sort(low: int, high: int) {
		var middle:int = 0

		if( low < high) do
			middle = low + (high - low) / 2

			merge_sort(low: low, high: middle)

			merge_sort(low: middle+1, high: high)

			merge(low: low, middle: middle, high: high)

		end
    }

	light() {
		var num1:int = 1
		var num2:int = 2
		var num3:int = 3
		var num4:int = 4


		print(arr)
		# tryy(low:0, high:3)
		merge_sort(low:0, high:3)
		print(arr)

		while(num1 < num4 and true)do
			num1 += 1
			print("ok")
		end

		# if(num1 > num4 and num3 > num1)do
		# 	print("if")
		# end
		# else do
		# 	print("else")
		# end

	}
}