program lolz {

	var numArrayGlob:[decimal](5)

    function tryArr() {
        var a:boolean = false
        var i:int
        var numArray:[int](5)

        print(numArray[10])

        for(i = 0; i < 5; i += 1)do

    		numArray[i] = i*2
    		print("YOU MOM")

    	end

        numArray[0] = 0

        numArray[numArray[0]] = numArray[0] + 2

        print(numArray[4])
        #print(numArrayGlob[0])
    }
  

    light() {

        tryArr()
    }
}