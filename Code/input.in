program lolz {

    var a, ab, abc:int
    var global:int = 0

    function example(posX: int, posY: int, posZ: decimal) returns int {

        if (1 < 2) do
            print("true")
        end

        print("This")
        return 0
    }

    function sumNumbers(first: int, last: int) returns int{
        var sum:int = 0
        for (sum = first; index < last; index += 1) do
            
        end
        print("true") 
        return sum
    }

    light() {

    }
}